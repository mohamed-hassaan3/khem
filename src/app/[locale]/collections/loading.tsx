import CollectionSkeleton from "@/src/components/ecommerce/CollectionSkeleton";

/** Overrides the tree-wide roundel: this route has a shape worth outlining. */
export default function Loading() {
  return <CollectionSkeleton />;
}
